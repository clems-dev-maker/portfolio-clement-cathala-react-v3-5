{
  "name": "portfolio-clement-cathala",
  "private": true,
  "version": "3.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check:build": "node scripts/verify-build.mjs",
    "check:seo": "node scripts/check-seo.mjs",
    "check:production": "node scripts/check-production.mjs",
    "check:content": "node scripts/check-content.mjs",
    "check:all": "npm run build && npm run check:build && npm run check:seo && npm run check:production && npm run check:content"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^5.4.10"
  }
}
